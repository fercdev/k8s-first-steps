apiVersion: v1
kind: ConfigMap

metadata:
  name: app-config

data:
  # Application configuration settings
  APP_NAME: "Demo Project"
  APP_ENV: "development"
  APP_DEBUG: "false"
  APP_PORT: "8080"
  LOG_LEVEL: "info"