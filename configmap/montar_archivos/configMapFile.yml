apiVersion: v1
kind: ConfigMap

metadata:
  name: nginx-config

data:
  index.html: |
    <h1>HOLA DESDE EL CONFIGMAP CUSTOM!</h1>
  mensaje.txt: |
    Este es un mensaje de prueba desde el ConfigMap.